export type CommonErrorType = {};

export type TodoType = {
  userId: number;
  title: string;
  completed: boolean;
};

export type PhotoType = {
  albumId: number;
  id: number;
  title: string;
  url: string;
  thumbnailUrl: string;
};
